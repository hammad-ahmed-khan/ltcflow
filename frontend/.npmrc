# emoji-mart@3 declares an old React peer range; the app runs React 18.
# This makes `npm install` use the legacy resolver by default so contributors
# (and CI) don't have to remember --legacy-peer-deps.
legacy-peer-deps=true
